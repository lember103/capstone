import {Box} from "@mui/material";

export default function Header(){
    return(
        <Box
            sx={{
                width: '100%',
                height: 100,
                backgroundColor: 'primary.dark',
                marginTop: -5
            }}
        />
    )
}